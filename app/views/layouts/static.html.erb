<%= inside_layout "application" do %>

  <div class="container">
    <div class="row">

      <% if title = yield(:title) %>
        <h1 class="text-center has-sub-header"><%= title %></h1>
        <hr>
      <% end %>

      <div class='col-lg-12 static-page-content'>
        <%= yield %>
      </div>

    </div>
  </div>

<% end %>
